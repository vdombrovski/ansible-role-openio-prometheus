---
- name: Install
  apt:
    name: prometheus
  register: install_packages
  until: install_packages is success
  retries: 5
  delay: 2
  ignore_errors: "{{ ansible_check_mode }}"
  tags: install
...
